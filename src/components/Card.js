import React from "react";
import styled from "styled-components";

const CardStyle = styled.div`
  background: #efefef;
  height: 400px;
  width: 250px;
`;

function Card() {
  return <CardStyle />;
}

export default Card;
